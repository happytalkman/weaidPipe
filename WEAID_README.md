# 🎙️ PipeAId - WEAID Real-Time Voice Engine

이 프로젝트는 **Pipecat** 프레임워크를 기반으로 구축된 **WEAID (에이드)** 전용 실시간 음성 및 멀티모달 대화 파이프라인 엔진입니다.

## 🌟 주요 특징 및 구성
1. **창조자 및 페르소나**:
   - 창조자: **이길환 (HAPPYTALKMAN)** 님
   - 어시스턴트: **WEAID (에이드)**
   - 헌법적 AI 원칙 및 한국어 실시간 구어체 최적화
2. **Gemini Live 양방향 음성 스트리밍**:
   - 음성 인식(STT), 두뇌(LLM), 음성 합성(TTS)이 한 세션에서 초저지연(Ultra-low latency)으로 동작
   - 동시 발화(Barge-in/Interruption) 지원: 사용자가 말하면 즉시 멈추고 경청
3. **WEAID 온톨로지 지식그래프 연동**:
   - 대화 턴이 발생할 때마다 `core/graph_store.py`를 통해 `memory/conversation_graph.json`에 누적 기록
4. **내장 도구 (Function Calling)**:
   - `get_current_time`: 현재 한국 표준시 조회
   - `get_weaid_status`: WEAID 어시스턴트 및 온톨로지 지식 통계 조회
   - `google_search`: 최신 정보 실시간 웹 검색

## 🚀 실행 방법
### 1. 웹 브라우저 (WebRTC 대화)
- 스크립트 실행:
  - PowerShell: `.\run.ps1`
  - CMD: `run.bat`
- 브라우저 접속: `http://localhost:7860`
- **Connect** 클릭 후 마이크 권한을 허용하면 바로 음성 대화가 시작됩니다.

### 2. 수동 실행 명령
```powershell
$env:PYTHONUTF8 = "1"
uv run python bot_weaid.py -t webrtc --port 7860
```

## 🎥 WeAid Meet 화상회의

서버 실행 후 `http://localhost:7860/meet`에 접속합니다. 다른 기기에서 접속할 때는
HTTPS와 WebSocket 업그레이드를 지원하는 프록시가 필요합니다.

1. 홈에서 브라우저 신원을 생성하고 참가자 이름과 회의 이름을 입력합니다.
2. 입장 전 미리보기에서 카메라·마이크를 확인하고 **참가 요청**을 누릅니다.
   장치 권한이 없어도 수신·채팅 전용으로 참가할 수 있습니다.
3. 첫 참가자가 호스트입니다. 이후 참가자는 대기실에서 기다리며, 호스트는
   **채팅·참가자** 패널에서 승인·거절하거나 회의를 잠글 수 있습니다.
   잠금은 새 참가 요청을 막으며 이미 대기 중인 요청은 호스트가 처리합니다.
4. **초대 링크**를 공유합니다. 링크에는 회의 이름만 포함되며 참가자 이름은 포함되지 않습니다.
5. 회의 중 마이크·카메라 제어, 화면 공유, 채팅, 참가자 목록, 손들기를 사용할 수 있습니다.
   화면 공유는 카메라 영상을 대체하며 시스템 소리는 공유하지 않습니다.

### 녹화와 자막

- **녹화**: 모든 참가자의 동의를 받은 뒤 녹화할 회의 탭·창·화면을 선택합니다.
  선택한 화면과 내 마이크·수신 중인 참가자 음성을 혼합해 이 기기에 저장합니다.
  시스템 소리는 녹음하지 않습니다. 녹화 상태는 회의에 표시됩니다.
  **녹화 저장** 또는 브라우저 공유 중지로 WebM/MP4 파일을 다운로드합니다.
  녹화는 메모리에 보관되며 30분 또는 약 256MB에서 자동 저장됩니다.
  페이지 새로고침·브라우저 종료 시 저장이 보장되지 않으므로 먼저 녹화를 저장하세요.
- **자막**: 한국어 브라우저 음성 인식을 지원하는 환경에서, 사용자가 동의하고
  자막을 켠 경우에만 내 음성을 인식해 참가자에게 공유합니다. 마이크를 끄면
  내 음성 인식도 중단됩니다. 각 참가자가 자신의 자막을 켜야 해당 발화가 공유됩니다.
  브라우저 공급자의 외부 음성 인식 서비스에 음성이 전달될 수 있습니다.
  지원하지 않는 브라우저에서도 다른 참가자가 공유한 자막은 표시할 수 있습니다.
  서버는 자막과 녹화 파일을 저장하지 않습니다.
- 화면 캡처·녹화·음성 인식 지원은 브라우저와 운영체제에 따라 다릅니다.
  미지원·권한 거부 시 안내를 표시하며 나머지 회의 기능은 사용할 수 있습니다.

### 연결 설정과 운영 범위

- 기본 연결은 Google STUN을 사용합니다. 서로 다른 NAT·회사망에서 연결하려면
  운영자가 TURN 서버를 준비하고 `WEAID_MEET_ICE_SERVERS` 환경 변수에
  WebRTC `RTCIceServer` 객체 배열을 JSON으로 지정해야 합니다.
  각 객체는 `urls`와 필요할 경우 `username`, `credential`을 사용합니다.
  이 값은 `/api/meet/config`에서 브라우저에 전달되므로 관리자 비밀키 대신
  제한된 수명·권한의 TURN 접속 자격 증명을 사용하세요.
- 회의 상태는 서버 프로세스 메모리에만 보관됩니다. 단일 워커로 실행하세요.
  마지막 참가자가 나가면 채팅과 대기 요청이 정리됩니다.
  호스트가 나가면 남은 참가자 중 가장 먼저 입장한 사람에게 권한을 넘깁니다.
  연결이 끊기면 명시적으로 다시 참가를 요청해야 합니다.
- 참가자 간 WebRTC 메시 연결을 사용하는 소규모 회의용입니다.
  대규모 회의에는 SFU와 분산 상태 저장 설계가 필요합니다.
- 브라우저 DID는 표시용 식별자이며 서버가 키 소유권을 검증하는 로그인 방식이 아닙니다.
  호스트 권한은 승인된 WebSocket 연결에 묶입니다. 같은 회의에서 동일 DID의
  동시 접속은 거부되므로 여러 참가자 테스트에는 서로 다른 브라우저 프로필을 사용하세요.
  회의 이름을 아는 사람은 참가 요청을 보낼 수 있으며 비어 있는 회의의 첫 참가자는
  호스트가 됩니다. 예약 회의 소유권·조직 인증·영구 회의 기록은 제공하지 않습니다.
